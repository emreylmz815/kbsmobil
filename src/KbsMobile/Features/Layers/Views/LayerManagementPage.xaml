<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KbsMobile.Features.Layers.Views.LayerManagementPage"
             Title="Katman Yönetimi">
    <VerticalStackLayout Padding="16" Spacing="8">
        <CollectionView ItemsSource="{Binding Layers}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="*,Auto" Padding="4">
                        <Label Text="{Binding Title}" VerticalOptions="Center"/>
                        <Switch Grid.Column="1" IsToggled="{Binding IsVisible}"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="Kaydet" Command="{Binding SaveCommand}"/>
    </VerticalStackLayout>
</ContentPage>
